# 人生战略游戏 · 项目总规格

**Coding Agent 最终定稿｜中文主体，英文代码术语｜2026-09-11**

本文件是项目需求与实现边界的 **Source of Truth**。适用目标为 Windows 11 本地个人应用 v1。后续实现不得从旧对话或旧规格重新引入本文件已排除的设计。

**Review 范围声明：**已按分页读取《设计人生决策游戏化需求》的全部可访问对话，共 27 个 turn，直至接口返回无更早内容，并对照本次用户提出的 19 项要求。原《人生战略游戏_项目总规格_CodingAgent版》仅以文件引用出现在对话中；接口未提供文件正文或可下载附件，当前工作目录也没有原文件。因此，本文件是依据完整可访问需求对话重建的最终规格，完成的是需求追溯、旧设计清理和新定稿一致性审查，**不声称完成原文件逐行 diff 或验证其全部未展示内容**。Review 说明与追溯表一并收入本文，不另交第二份报告。

## 1. 阅读规则与需求优先级

1. 本次 19 项明确要求优先于原对话。
2. 原对话中较晚的用户明确修正覆盖较早回答。
3. 用户明确同意，以及后续讨论确实建立在其上的建议，视为有效需求；仅未被反对、但没有后续承接的建议不自动成为要求。
4. 本文标为“实现约定”的内容，是让已确认需求可落地的工程选择，不冒充用户原话；可替换实现，但不得改变产品行为。
5. “未来可做”“不属于 v1”不构成 v1 交付义务。开发阶段划分也不意味着可以遗漏 v1 最终验收项。

## 2. 产品定位与不可违反的原则

产品保存用户对人生的战略理解：长期政策、当前境况、自我认识、重要历史与未来可能性。它通过类似 HOI4 的国策、领导人、国家精神、事件和总参谋部，提供叙事感、决策参考与趣味。

主要用户是本人。不要求用户录入完整决策过程或每天的流水账；用户可以只记录结果，也可以在软件外使用 AI，再把确认的文字写入软件。

| 编号 | 原则 | 实现后果 |
|---|---|---|
| P01 | 用户掌握状态与叙事权 | 开始、完成、暂停、撤销、归档以及数值调整由用户操作 |
| P02 | AI 按钮制 | 不在后台定期分析、规划或修改人生；每次调用有明确入口 |
| P03 | AI 建议先预览、后接受 | 未接受的候选不得进入正式数据库、画布或档案 |
| P04 | 关系表达用户理解 | 前置、互斥、演化、来源均不升级为现实规则引擎 |
| P05 | 低维护负担 | 不要求日更、打卡、完整决策过程、资源预算或精细技能统计 |
| P06 | 历史可回看，错误可纠正 | 必要操作历史保留；归档与删除并存；快照由用户主动创建 |
| P07 | 本地优先 | 人生数据保存在本机；云端 LLM 仅接收用户允许的上下文 |
| P08 | 叙事不改事实 | 风格用于生成与润色；最终只保留用户确认正文 |

不设系统定义的人生胜利条件，不自动评判用户是否成功、失败或自相矛盾。

## 3. v1 范围

### 3.1 必须交付

- 五个核心模块：国策、领导人、意识形态、人生哲学、国家精神。
- 当前局势、稳定度与变更历史、独立决议、普通事件与超事件、Markdown 随笔。
- 单个 Life 内的大二维国策画布：拖拽、连线、前置、互斥、多选、缩放、平移、网格吸附与对齐辅助。
- 用户主动命名的世界快照、文本保存快照入口、只读回溯、统一 Archive。
- 云端 LLM 配置、provider adapter、生成/润色/分析预览、下一国策候选、总参谋部多轮讨论。
- 底层多 Life 隔离；v1 默认进入一个 Life，提供简洁的创建、命名、切换入口作为实现约定，不做账号体系。
- SQLite、Repository/Service、migrations、本地附件、手动备份与恢复、可关闭的自动备份、Markdown/JSON 导出。
- 普通文本自动保存反馈；国策画布撤销；涉及 AI 接受、状态变更和删除的明确用户操作。

### 3.2 不属于 v1

跨设备同步、多平台同时发布、账号/社交/多人协作、完整 Mod 或插件运行时、脚本控制台、自定义首页 Widget 编辑器、复杂动画系统、全局搜索与独立历史页面、时代划分、独立未来推演实体、资源经济与属性奖励系统、自动随机任务或惩罚、隐藏国策与解锁机制、国策树版本分支系统。

保留通用 ID、清晰分层、可迁移数据、配置化资源和导出能力即可；不得为未来同步提前实现服务器、冲突合并、CRDT、同步队列或账号权限系统。

## 4. 领域词汇与总体结构

| 中文 | 代码术语 | 定义 |
|---|---|---|
| 人生空间 | `Life` | 一个独立人生世界，具有独立数据和国策画布 |
| 世界快照／人生存档 | `WorldSnapshot` | 用户主动保存并命名的某个 Life 当时的世界状态 |
| 文本快照 | `TextSnapshot` | 用户主动保存的某段正文版本；不是每次编辑的自动版本 |
| 档案库 | `Archive` | 聚合各领域历史内容的展示与查询层，不是存储副本的表 |
| 国策 | `Focus` | 用户设定的重要、长期政策节点，只有一种统一粒度类型 |
| 国策关系 | `FocusRelation` | 用户建立的前置或互斥关系 |
| 决议 | `Decision` | 独立待办小事项；不是决策过程，也不是国策子任务 |
| 决议完成记录 | `DecisionOccurrence` | 用户一次“做了”的记录，重复决议可有任意多条 |
| 领导人／特质 | `Leader` / `Trait` | 自我描述与卡片式长期特征 |
| 意识形态 | `Ideology` | 用户自定义的思想与价值取向条目，可多个并存 |
| 人生哲学 | `Philosophy` | 一篇长期维护的 Markdown 文档 |
| 国家精神 | `NationalSpirit` | 外部环境、阶段状态和现实约束卡片 |
| 当前局势 | `Situation` | 用户对当下整体境况的文字概述 |
| 稳定度 | `Stability` | 用户解释与维护的主观数值 |
| 事件／随笔 | `Event` / `Essay` | 重要发生事项的记录／自由 Markdown 文字 |
| 总参谋部 | `Staff` | 固定可配置的 AI 成员及用户发起的讨论 |
| 会议 | `StaffMeeting` | 用户确认保存的讨论记录或会议纪要 |
| AI 候选 | `Proposal` | 仅存在于本次预览会话中的待确认内容 |

每个 Life 具有自己的领导人、当前局势、人生哲学、稳定度、Trait、意识形态、国家精神、国策与关系、决议、事件、随笔、会议和快照。通用应用设置、provider 配置与秘密存储不属于某个人生世界。

## 5. 首页与视觉交互

首页是叙事仪表盘。以 HOI4 的信息密度、政治面板气质和层次关系为参考，中央展示人生局势与关键状态，不放地图作为功能前提。

首页应能看到领导人、稳定度、当前局势、进行中国策、国家精神、待执行决议与最近重要事件，并能进入意识形态、人生哲学、国策画布、档案和总参谋部。核心五模块保持清晰入口，其余提供配套能力。

v1 固定布局，具体排版由设计/开发确定，先保证视觉风格、文字可读性和信息层次，再做复杂动画。不能为了游戏气氛隐藏保存失败、选中状态、真实稳定度或只读快照提示。

界面面向普通用户，显示中文领域名称；表名、事务、adapter 等实现术语不进入日常操作流程。资源和样式集中管理，使用项目自身资产；这不要求实现 Mod 加载器。

## 6. 自我与当前世界

### 6.1 领导人和 Trait

每个 Life 一个当前领导人，包含名称、头像和自由 Markdown 描述。用户描述自己的思维模式、性格与长期状态，允许娱乐性表达。

Trait 使用卡片，包括标题、图标或图片、正文、当前展示状态和可选来源关联。**不设置正面／中性／负面分类、分值、经验或自动等级。**

Trait 演化由用户主动建立 `A → B`。查看 B 可追溯前身 A；建立关系本身不自动归档 A，也不自动启用 B。v1 实现简单的前身—后继关系和谱系展示，不开发 `A + B → C` 合流机制。是否支持分叉不作为 v1 必须项。

旧 Trait 可归档并在档案或谱系中查看。用户可以删除写错的 Trait；删除与历史引用的处理遵循第 15 节。

### 6.2 意识形态

支持多个并存，每项为图标、名称和 Markdown 描述，内容由用户定义。不设默认主意识形态，不强制维度、比例、阵营或模型评价。

允许调整展示顺序、编辑、归档和主动保存文本/世界快照。编辑后可提供非强制的“记录为事件”入口；没有自动创建事件或自动冻结世界。

### 6.3 人生哲学与当前局势

人生哲学是每个 Life 一篇长期 Markdown 文档，可用标题组织章节，不拆成哲学条目体系。

当前局势也是独立长期文字，描述用户认为重要的当前境况，不由国家精神自动拼接产生。可按 AI 按钮请求总结或润色，用户接受后才替换正文。

两者与领导人、意识形态彼此独立；修改任何一项都不自动同步修改其他项。均支持用户主动保存文本快照和世界快照。

### 6.4 国家精神

由用户维护标题、图标/图片、正文和可选来源。表示外部环境、阶段性条件和约束，不承担细颗粒度技能积累。

**不设置正／中／负分类，不设自动 modifier 效果。**用户即使在文字中写“学习效率 +10%”，也只是叙事文本，不参与任何计算。

从当前面板移除时使用“归档”，数据保留，当前面板不显示。避免给用户同时提供含义相近的 inactive 与 archive 两套概念。归档可恢复；写错可删除。

不要求用户填写起止日期或持续时间。数据库技术时间戳不等于产品要求用户维护的业务日期。

### 6.5 稳定度

稳定度由用户直接输入当前值，或手动输入变化量并确认结果。可选填写原因及来源国策、事件等。

- 不限于 0–100，允许 `135`、`-20`；界面显示真实值，不截断、不隐藏超界部分。
- 不因决议未做、国策完成、事件出现、拒绝建议或时间流逝而自动改变。
- 输入变化量后计算预览只是算术辅助，确认前不能保存。
- 每次确认保存 `before_value`、`after_value`、`delta`、时间、可选原因与来源，支持回溯和历史曲线。
- 数值必须有限且可解析；拒绝 NaN/Infinity 属于数据校验，不是人生数值规则。
- 初始值不由 AI 推断。实现约定：首次创建 Life 由用户填写，可暂留未设置状态，不偷偷赋予评价。

首次从未设置变为数值时，历史的 `before_value` 与 `delta` 为 null，只记录设定结果；尚无基准值时不提供“增减”操作，不能把未设置当作 0。

## 7. 国策 Focus

### 7.1 单个大二维画布

每个 Life 只有一个大二维国策画布，可有多组不相连的节点。顶层国策标题自然形成区域语义。

**不存在命名 `FocusTree` 实体、`focus_tree` 表、`focus_tree_id` 字段或树选择器。**核心持久化为 `focus` 与 `focus_relation`。视口设置属于 Life 的界面偏好；未来分区也不能偷偷变成领域树实体。

节点支持标题、图标、图片、Markdown 正文、状态、坐标、状态历史和可选关联。用户自由拖动，手动连线，支持网格吸附、对齐、多选、缩放与平移。重新打开后保留布局。

### 7.2 状态与日期

只有四种状态：

```ts
type FocusStatus = 'active' | 'completed' | 'paused' | 'revoked';
```

分别显示“进行中／完成／暂停／撤销”。没有“计划中”“锁定”“隐藏”或单独的正式候选状态。允许同时进行多个国策。

任意状态可由用户转换到另外任一状态，包括完成后重开、撤销后恢复。不因日期、前置或互斥关系阻止操作。

每次真实状态改变保存状态历史：原状态、新状态、发生时间、记录时间、可选原因和来源。暂停、撤销允许写原因但不强制。新建时记录初始状态。重复点击同一状态不制造虚假转换。

可展示开始、暂停、恢复、完成、撤销等时间及当前进行时段的已持续天数。实现约定：时间显示从状态历史派生，避免仅靠一个 `completed_at` 字段丢失多次完成/重开事实。

不设预计完成时间、35/70 天规则、自动完成度或完成百分比。用户可自行把大国策拆成若干国策节点，拆分不引入新的节点类型或 Decision 子任务。

完成、暂停、撤销均不让节点自动消失。通过边框、颜色、图标或盖章表达状态，让画布保留战略历史。用户主动删除错误节点是独立行为。

### 7.3 前置和互斥

前置关系有方向，表示用户认为某国策是另一国策的基础。它只是关系，不是解锁条件，不阻止启动或完成。

互斥关系无方向，表示“用户认为两条路线存在冲突”。软件负责显示连线与用户说明，**不自动判定现实冲突、不因同时进行或同时完成弹出战略矛盾警告、不阻止操作，也不自动取消另一条路线**。

用户主动点击 AI 分析时，AI 可以评论冲突，但评论仅是建议，不自动创建互斥线。用户确认添加关系后才落库。

只做结构有效性校验：端点存在、同属当前 Life、不连自己、同类型关系不重复。v1 不增加现实冲突检测、前置满足计算或额外人生逻辑限制。

### 7.4 AI 尝试生成下一节点

1. 用户在现有国策节点上点击“AI 尝试生成下一节点”。
2. 显示本次锚点、将发送的相关前置关系及所选模块上下文；仅发送用户允许的数据。
3. AI 生成一个或多个可能的后续国策。不同候选可以代表继续、转向或暂停整顿等方向；不承诺结果。
4. 在独立预览区显示候选标题、正文、图片建议及建议关系。**候选不进入正式画布，不写入任何数据库表。**
5. 用户逐个修改、接受或丢弃，可以接受多个，也可以全部丢弃。
6. 接受时用户确认标题、正文、四种状态之一及要建立的关系。不得默认把候选视为已开始，不为解决该问题新增“计划中”。
7. Service 重新校验锚点与 Life，单次事务保存该次接受的节点、关系及初始状态历史，成功后出现在画布。

AI 不自动生成整棵未来人生树，不递归调用“下一节点”，不在接受后自动继续续写。用户仍可手工新建，也可输入关键信息按模板生成单个新国策候选；空白处生成不改变节点续写的主要入口。

未接受 Proposal 仅保留在本次应用内存；关闭或丢弃即清除，不进入 SQLite、文件缓存、备份、Snapshot、Archive 或正文日志。接受时若锚点已删除、Life 已切换或状态已变化，须让用户重新核对，不静默写入另一个人生空间。

## 8. 决议 Decision

决议是 Todo 小事项，与国策并列。字段包括名称、描述、用户分类、一次性/重复类型、可选目标日期时间及状态。没有 `focus_id` 父任务字段、国策子任务列表或决策分析流程。

决议没有执行条件、资源成本、冷却、自动解锁、周期或逾期惩罚。可选时间只作为用户提示信息，不产生任务调度。

| 类型／操作 | 行为 |
|---|---|
| 一次性，点击完成 | 新增一次完成记录，状态变为已完成 |
| 重复，点击完成 | 新增一次完成记录，累计次数加一，仍保持可执行 |
| 任一类型，点击放弃 | 表示此决议以后不再做，保留此前完成次数与放弃历史 |
| 未点击完成 | 表示没有记录“做了”，不自动写一条失败或放弃记录 |

重复决议可以无限次完成，同一分钟、同一天均可多次。没有“每日一期”“下次执行时间”“错过一次”“签到连续天数”。完成次数来自有效 `DecisionOccurrence` 数量，不另维护容易失配的权威计数器。

实现约定：决议状态为 `open/completed/abandoned`；重复决议完成一次仍为 `open`。放弃后不再直接计次；若用户误操作，提供明确“重新启用”并保存状态变更记录。一次性决议也可纠正误完成，历史不得被悄悄覆盖。

误记的一次完成可由用户明确作废，记录 `voided_at` 和可选原因，累计次数排除作废记录，历史中保留纠正痕迹。重新启用本身不清零过去真实完成次数；纠正一次性决议的误完成时，在同一事务中作废该次记录并恢复可执行状态。

操作历史记录“做了哪些、放弃了哪些”，不要求填写为何选择或完整思考过程。允许用户主动关联随笔、事件作为补充，但不引入父子任务语义。完成或放弃决议均不自动修改国策、稳定度、Trait 或国家精神。

## 9. 事件、超事件与随笔

### 9.1 事件

Event 包含标题、现实发生日期、最终 Markdown 正文、`normal/super` 类型、可选图片、引言、关联国策及 Snapshot。事件可独立存在，不强制关联国策。

**事件本身不自动施加效果。**不设置可执行的 `effects`、触发器、脚本或“记录事件即批量改状态”的工作流。

正确流程为：用户先记录事件，再到相关模块手动修改 Trait、意识形态、稳定度、国策状态等；每次修改可把该事件选为来源或关联。不要求所有修改发生在同一时刻，也不强制修改任何状态。

例如“转专业”事件保存后，不会自动撤销旧国策或改变意识形态。用户之后分别操作，并在需要时注明来源“转专业”。改写或删除事件也不会反向回滚已经发生的用户操作。

创建事件时可主动勾选保存当下世界快照，并命名。默认不创建快照。若快照在状态修改前创建，它冻结修改前世界；后续手动修改后需要新快照，不能更新旧快照冒充修改后的状态。

### 9.2 超事件

超事件和普通事件的领域语义相同，主要区别是视觉与叙事权重。v1 提供大图、标题、日期、可选引言与全屏/大弹层呈现；复杂动画和自定义音频编辑不作为 v1 验收前提。

用户图片可用于普通事件与超事件。关闭或重播演出不触发状态改变，不重复创建事件或快照。未确认的 AI 事件候选不能被当作已经发生的历史。

### 9.3 随笔

Essay 为标题与 Markdown 正文，可包含图片，可独立存在，也可关联国策、事件、领导人、Trait、意识形态、哲学、国家精神、当前局势、决议、快照或会议。

未来推演、复盘、想法与决策思考可以保存在随笔中，不另建预测引擎、概率模型或决策过程对象。AI 推演以定性可能性为默认表达，不制造精确成功率。

一个随笔可关联多个对象，关联通过 ID 保存，不复制随笔正文。用户是否写、写多少、是否允许进入 AI 上下文均由用户控制。

## 10. Archive：统一历史展示层

Archive 聚合以下已有数据：普通/超事件、世界与文本快照、随笔、已确认会议纪要、决议完成/放弃历史、国策状态历史，以及归档的 Trait、意识形态、国家精神等。

```text
各领域对象及历史表
        ↓ ArchiveQueryService
统一类型标识、时间、摘要与源对象引用
        ↓
Archive 卡片流与类型筛选
```

**不得创建把这些内容再复制一份的 `archive` 实体表。**可以在查询层进行 UNION、映射与分页，或用不持有独立副本的数据库 VIEW。每张卡片指向原领域记录，由领域 Service 执行编辑或删除。

实现约定：默认按业务时间倒序，超事件用更显著卡片样式，必要时同一日期内提高展示权重；不得为了类型优先而把总体时间顺序弄乱。提供轻量类型筛选，不做全局搜索或第二个独立历史页面。

Archive 卡片是展示模型，不是领域记录。不产生“归档复制成功但原记录未更新”的双写问题。历史多时分页加载，不要求用户自己整理文件夹。

“归档对象”意味着修改该对象的展示状态；“查看档案”意味着查询现有数据。Archive 不自动把所有文本编辑变成永久历史。

## 11. Snapshot、文本版本与现实时间

### 11.1 世界快照

WorldSnapshot 是用户主动创建、命名、可附描述、可删除的世界冻结状态，属于某一个 Life。不是新的 Life，也不是整个数据库物理备份。

保存时在一致性读取边界内冻结：领导人、当前与归档 Trait 及演化关系、意识形态、人生哲学、当前局势、国家精神、稳定度、全部国策及坐标/关系/当时状态，以及当前决议定义与当时状态/完成次数。字段应明确哪些属于当时世界，不能只存活对象 ID 后再读最新正文。

快照不递归复制已有 Snapshot，不复制整套 Archive、历次决议完成明细、事件库、随笔库或会议库。历史内容已有自己的领域存储；“世界全量冻结”指当时世界状态的完整性，不代表复制整个历史库。

快照引用的图片必须保留对应版本：实现约定为不可覆盖的附件文件加引用保留，附件清理时检查快照引用。原对象编辑、归档或删除后，旧快照仍能还原保存时的世界状态与相关图片。

结构包含 `snapshot_schema_version`、`life_id`、名称、描述、创建时间和冻结 payload。升级时兼容旧版本读取，不把旧快照自动改成当前状态。

### 11.2 文本快照

所有正文编辑入口提供主动“保存文本快照”能力，包括事件、随笔、会议纪要等不包含在世界 payload 中的正文。保存明确的正文、对象类型、来源 ID、名称/说明与时间。

普通文本编辑默认覆盖当前正文，不自动永久保存每次旧版本。文本快照是用户选择留下的历史稿，**不等于要求系统双存原始事实与风格版**。世界快照与文本快照都只冻结已确认保存的内容，不能捕获 AI 待接受候选。

### 11.3 回溯与恢复边界

采用现实时间，不做游戏时钟、自动推进时代或任意日期全世界重建。用户打开某个快照进入醒目的只读“查看当时状态”，退出后回到当前 Life，不改变当前数据。

原对话没有最终确认“一键把旧世界快照覆盖为当前世界”。v1 不实现此操作，也不把“可回溯”解释为已授权覆盖恢复。**备份恢复是第 16 节独立的数据安全功能，属于 v1。**

## 12. AI、叙事风格与总参谋部

### 12.1 通用 AI 流程

```text
用户点击 → 选择上下文与写作风格 → ContextBuilder
→ ProviderAdapter → 内存 Proposal → 用户修改／接受／丢弃
→ Service 校验与保存 → UI 更新
```

ProviderAdapter 不持有写入人生数据库的能力。模型输出是待确认数据，不是命令；解析成功也不等于用户接受。生成、润色、分析、反驳、总结、定性推演均遵循按钮制。

API 调用失败、取消、输出格式不合格、返回空内容时，不覆盖正文、不创建空对象。错误提示允许用户手动重试；不得静默重试引起多轮费用或重复接受。

AI 调用可以为某一次已启动会议按轮数连续请求，但不得在会议结束后自动开启新会议、推进国策或发起后台分析。

### 12.2 上下文控制

v1 按模块勾选：当前局势、领导人、Trait、意识形态、人生哲学、国家精神、稳定度、国策、决议、档案、随笔。模块重叠时按对象 ID 去重。

用户确认发送范围后 ContextBuilder 只查询当前 Life。展示模块、数据量以及实际将发送的文本预览；不做逐一勾选成百上千对象的选择器。历史是否进入上下文由档案等模块开关控制。

实现约定：选中模块数据超出模型可用上下文时，发送前明确提示需缩小范围或采用可见的截取方案，不悄悄扩大授权范围，不声称模型读过未发送内容。图片 v1 默认不上传，仅发送正文及必要元信息；不得因本地有附件就自动上传。

锚点国策和必要关系也须在本次发送预览中可见。查询结果、模型输出及 UI 临时状态均绑定 Life；切换 Life 时清除未接受候选，不能将前一个 Life 的资料带入下一次调用。

### 12.3 叙事风格

用户在生成或润色时选择风格，例如战略史书、冷静描述、公文或史诗。风格本质是 Prompt 配置，不是随时切换所有正文的渲染模式。

流程：用户输入关键事实 → 选择风格并点击生成/润色 → 预览并修改 → 接受 → **最终定稿成为唯一正式正文**。

不要求保存事实原文、多个风格版本或每次 AI 回复。用户自行创建的文本快照仍可保留，但不能把“可手动留版本”变成“系统强制双存”。模型不得补造人物、结果、日期等未提供事实。

### 12.4 总参谋部

用户维护固定成员。每个成员包含名称、职责、人格/立场 Prompt、启用状态及 provider/model 配置。提供模板和自定义能力。成员可使用不同模型，允许意见相互冲突。

发起会议时用户输入议题、选择成员、按模块选上下文，并设置轮数。用户决定讨论轮数，程序验证其在配置的安全上限内；具体上限为实现配置，不能把历史举例“10 轮”写成用户唯一指定值。

会议必须支持成员真正读取本次会议中此前发言并回应，不得仅把若干独立回答拼接冒充互相讨论。实现约定：按成员顺序逐轮发言，后续成员可看到此前发言；完成指定轮数后生成纪要候选，汇总共识、分歧、风险和未解决问题。

用户可以停止。停止或失败时保留当前界面的已完成发言供查看，不继续消耗调用；用户可选择保存不完整会议并标明状态。正常完成也只生成候选纪要，接受后才进入 StaffMeeting 和 Archive。

正文只保存用户确认的最终纪要，不强制保留原始输入与风格版。若用户明确选择保留讨论记录，可连同已确认的发言保存；默认不持久化整场原始响应。保存讨论记录是独立用户选择，不是叙事双存要求。

参谋可引用本次实际发送的历史资料，不得假装知道未授权或不存在的记录。会议不要求用户记录最终人生决定，不建立强制表决、决策锁定或自动执行流程。

### 12.5 模型配置与秘密

v1 支持配置云端 provider、模型及必要连接参数；成员能绑定不同配置。采用统一 adapter 接口，可先交付一个真实可用 provider，同时验证配置与分派不锁死单一模型。更多厂商的协议适配按实际使用加入，不要求 v1 为所有厂商实现 SDK。

API Key 放本机安全秘密存储，不放 Life、SQLite 人生数据、Snapshot、Markdown/JSON 导出或备份。UI 不回显完整密钥，日志不记录密钥、完整上下文或候选正文。密钥持久化不可用时允许会话内输入，不能退化为明文塞入人生数据库。

## 13. 技术架构与职责

采用 **Tauri 2 + React + TypeScript + SQLite + React Flow**，配套 Repository/Service、migrations 和 provider adapter。此处锁定技术方向，不锁定未经安装验证的依赖小版本或具体插件 API。

| 组成 | 职责与采用理由 |
|---|---|
| Tauri 2 | Windows 桌面容器、本地文件与数据库边界、秘密存储和云 API 调用入口 |
| React + TypeScript | 构建高信息密度面板和自定义卡片；显式表达领域类型 |
| React Flow | 承担图编辑交互，项目实现国策节点、连线语义与状态视觉 |
| SQLite | 保存本地结构数据及 Markdown TEXT，关系通过 ID 连接 |
| Repository | 封装如何查询和写入，统一 Life 过滤，组件不得直接写 SQL |
| Service | 处理用户操作、领域校验、事务及历史；不自动推导人生效果 |
| migrations | 升级旧数据库结构，保留用户已有数据 |
| provider adapter | 隔离厂商协议，向上输出统一候选与错误，不访问领域写接口 |
| 本地附件目录 | 保存图片文件；数据库存 ID、相对路径和元数据 |

实现约定：领域写入通过 Tauri 命令进入可信本地服务边界；Repository 和 Service 可以组织在后端应用层。前端只传明确 DTO，不传任意 SQL。若调整具体代码语言或文件组织，仍须保持唯一业务写入边界和事务归属。

建议按 feature 划分 UI（focus、leader、decision、archive、staff 等），共享领域 DTO；后端按 services、repositories、providers、storage、migrations 组织。无需依赖注入框架、通用工作流引擎或微服务。

UI 状态仅管理选中项、弹窗、编辑缓冲、视口与 Proposal；持久化世界状态以 SQLite 为准。可使用轻量状态管理方案，不把某个额外库当成用户指定要求。

## 14. 数据模型与数据库约束

### 14.1 通用约定

以下为逻辑 Schema，字段名可由 migration 细化，但语义不得削弱。

- 对象使用 UUID；可变对象包含 `created_at/updated_at`，历史记录包含发生时间和记录时间。现实发生时间可与录入时间不同。
- Life 所属领域记录均有 `life_id`，关联表两端必须同 Life；Repository 方法显式接收 `lifeId`，不能只凭对象 ID 越界访问。
- 单例对象以 `life_id` 唯一；关系使用 ID，不依赖可改名的标题。
- 重要可查询字段正规化，Markdown 为 TEXT。快照 payload 和明确配置可用带版本的 JSON，不能把全部数据库变成一列 JSON。
- foreign keys、唯一约束、必要索引与事务从 v1 建立。跨对象泛型关联由 Service 校验类型、存在性与 Life，不能误以为字符串类型字段获得了数据库外键保证。
- 时间以明确时区约定存储；仅日期的事件保留日期精度，不补造具体时刻。排序使用稳定的次级键。

### 14.2 逻辑表清单

| 表／对象 | 核心字段或职责 |
|---|---|
| `life` | id、name、created_at、updated_at |
| `leader` | id、life_id UNIQUE、name、portrait_attachment_id、body_md |
| `situation` / `philosophy` | id、life_id UNIQUE、body_md |
| `trait` | id、life_id、title、body_md、icon/image、archived_at |
| `trait_relation` | id、life_id、predecessor_id、successor_id、occurred_at、note；v1 无合流 |
| `ideology` | id、life_id、title、body_md、icon/image、sort_order、archived_at |
| `national_spirit` | id、life_id、title、body_md、icon/image、archived_at；无正负分类和效果字段 |
| `stability` | life_id UNIQUE、current_value 可未设置 |
| `stability_change` | id、life_id、before_value、after_value、delta、occurred_at、reason、source 引用 |
| `focus` | id、life_id、title、body_md、icon/image、status、position_x、position_y |
| `focus_relation` | id、life_id、source_focus_id、target_focus_id、type、note |
| `focus_status_history` | id、life_id、focus_id、from_status、to_status、occurred_at、recorded_at、reason、source |
| `decision` | id、life_id、title、body_md、category、kind、status、optional_target_time |
| `decision_occurrence` | id、life_id、decision_id、occurred_at、recorded_at、optional_note、voided_at、void_reason |
| `decision_status_history` | id、life_id、decision_id、from_status、to_status、occurred_at、reason |
| `event` | id、life_id、title、body_md、kind、occurred_on、image、quote、optional_snapshot_id |
| `essay` | id、life_id、title、body_md |
| `world_snapshot` | id、life_id、name、description、snapshot_schema_version、payload_json、created_at |
| `text_snapshot` | id、life_id、source_type/id、name、description、body_md、created_at |
| `staff_member` | id、life_id、name、role、prompt、provider_config_id、model、enabled、sort_order |
| `staff_meeting` | id、life_id、topic、confirmed_minutes_md、context_module_names、rounds、status、created_at |
| `staff_message`（用户选存时） | id、life_id、meeting_id、speaker_label、round_index、confirmed_content |
| `object_link` | id、life_id、source_type/id、target_type/id、kind；随笔挂载和可选来源 |
| `attachment` / `attachment_link` | ID、Life、相对路径、MIME、hash、大小、所属对象；快照引用也计入保留 |
| `provider_config` | 应用级连接配置及 secret_ref，不保存密钥值 |
| `app_setting` | 当前 Life、备份开关、界面偏好、配置版本；不放正式人生正文 |
| migration 元数据 | 已应用的迁移版本与校验信息 |

明确不存在：`focus_tree`、Archive 副本表、持久化 `ai_proposal` 表、重复决议调度表、决议父国策列、Trait/NationalSpirit 正负属性列、事件自动效果表、跨设备同步表。

互斥关系使用端点规范化和唯一约束去重；前置保留方向。同一对节点存在不同用户关系时不自动推断矛盾。删除端点时按第 15 节处理当前关系，不级联抹除已冻结快照。

### 14.3 必须保持的事务边界

| 用户操作 | 原子保存内容 |
|---|---|
| 改国策状态 | 当前状态 + 一条状态历史 |
| 改稳定度 | 当前值 + 一条变更记录 |
| 完成决议 | 一条完成记录 + 必要状态/状态历史变化 |
| 放弃或重新启用决议 | 状态 + 状态历史 |
| 接受下一节点候选 | 接受的 Focus + 确认的关系 + 初始状态历史 |
| 保存世界快照 | 一致状态 payload + 元数据 + 附件引用 |

使用操作请求 ID 或等效去重防止同一次按钮请求因重试产生两条记录；不得用“同一天只能完成一次”去防重，因为用户可以真实地多次完成重复决议。

## 15. 保存、归档、删除与撤销

普通正式正文编辑自动保存，并显示保存中、已保存、失败状态；失败时保留编辑缓冲并允许重试。关闭/切换前处理尚未成功保存的文字。

AI 预览编辑不参与正式正文自动保存。只有“接受”将候选转换为正式正文；之后的用户编辑才进入普通自动保存。保存文字本身不自动创建历史快照。

归档与删除并存：归档保留对象并退出当前展示，可恢复；删除用于写错等纠正。删除操作说明范围并由用户确认。不得把“归档”当成满足“删除”需求的唯一替代。

实现约定：v1 提供真正删除当前对象的能力。删除前展示关联影响；当前关系删除或解除，来源引用可变为“来源已删除”，但不得回滚当时的状态变化或稳定度。需要保留历史可读性时，历史记录保存当时标题标签。

删除当前对象不自动删除独立 Snapshot、TextSnapshot 或已有备份中的副本；用户可分别删除这些保存件。正文应明确此边界，不承诺单次对象删除会擦除所有历史副本。删除一个 Life 的全空间批量入口不作为 v1 必须功能。

国策画布支持 Ctrl+Z 撤销移动、连线编辑和最近删除；实现约定为会话内撤销栈，不承诺跨重启撤销。撤销持久化操作也经 Service 校验并同步数据库，不能只改变屏幕。画布撤销不撤销已发生的 LLM API 请求费用或另一个模块的独立状态操作。

## 16. 数据安全、备份、恢复与导出

SQLite 是正式结构数据和 Markdown 正文的单一真相源；附件保存在管理目录。界面引用相对管理路径，导入图片复制为受管理文件，不能依赖原始文件永远留在桌面。

备份应包含一致的数据库、附件和格式/Schema 版本清单，不含 API Key。使用数据库支持的一致备份方式或经过验证的关闭写入流程，不能在数据库写入期间只复制主文件并忽略事务状态。

手动备份和恢复是 v1 必须能力。自动备份可在设置中开关，显示最近结果和失败原因；具体频率、保留数量为配置约定，不写成用户已指定值。Snapshot 不替代备份，备份也不出现在世界快照列表冒充人生节点。

恢复流程：选择备份 → 校验版本、完整性及附件 → 展示覆盖范围 → 创建恢复前备份 → 用户确认 → 排他恢复 → 重新打开并验证。校验失败不得破坏当前数据；恢复失败须能回到恢复前状态。不以备份恢复实现未确认的 Snapshot 覆盖世界功能。

提供 Markdown 可读导出与带格式版本的 JSON 数据导出，包含对象 ID 和关系，按所选 Life 限定范围。导出不包含密钥、未接受候选或其他 Life 数据。Markdown 用于脱离软件阅读，JSON 用于结构化数据携带；普通导出不等于完整数据库备份，也不把通用 JSON 合并导入扩成 v1 必须项。

附件清理必须检查当前对象、文本/世界快照等引用；旧备份按自己的保留策略独立处理，不因当前对象删除而自动损坏旧备份。

迁移失败不得删除数据库重新初始化；迁移前备份，迁移在可恢复边界执行。未知更高版本数据库/快照应明确拒绝不安全写入或显示不兼容提示，不猜测解析。

## 17. 开发顺序

| 阶段 | 交付重点 | 阶段出口 |
|---|---|---|
| Phase 0 | 工程、领域类型、SQLite/migrations、Life、Service/Repository、附件与秘密边界 | 两个 Life 隔离验证，旧库迁移保数据，Windows 可启动 |
| Phase 1 | 五核心模块、当前局势、稳定度、国策画布与历史 | 全手工工作流可用，重启后状态/坐标正确，无自动人生效果 |
| Phase 2 | 决议、事件/超事件、随笔、Archive、Snapshot、备份恢复导出 | 可形成与回看真实档案，完成实际备份恢复验证 |
| Phase 3 | provider adapter、上下文、Proposal、下一节点、叙事与总参谋部 | AI 接受前零持久化，模型故障不破坏数据，按模块和 Life 隔离 |
| Phase 4 | 首页与视觉整合、异常处理、安装与全流程验收 | 本节各阶段能力共同满足第 18 节 |

可先让 Coding Agent 完成 Phase 0 + Phase 1 作为阶段里程碑，但不能把该里程碑称为已完成整个 v1。每阶段先验证领域行为再完善表现；无需等 Mod、同步或复杂动画才发布 v1。

## 18. 验收标准与 Definition of Done

以下是实现后须验证的行为，不表示本次文档 Review 已运行应用测试。

| ID | 场景 | 通过条件 |
|---|---|---|
| A01 | 创建 Life A/B 并交替编辑 | 对象、关系、查询、导出、上下文和快照不串 Life |
| A02 | 查看五个核心模块 | 独立含义与入口清晰，正文可手工维护，无 AI 使用前提 |
| A03 | 新建、拖拽、连线后重启 | 单画布节点和坐标保持；无命名树实体 |
| A04 | 前置未完成，启动后继 | 允许操作，仅表达关系 |
| A05 | 互斥节点同时进行、同时完成 | 允许，保留互斥线，不自动警告或取消 |
| A06 | 四状态往返及多国策同时进行 | 全部允许，历史时间和原因可查；无计划中或自动进度 |
| A07 | 保存事件再改标题/删除事件 | 不改变任何 Trait、意识形态、稳定度或国策状态 |
| A08 | 保存事件后手动修改各模块并引用事件 | 只有用户操作生效；来源可查；事件不是批量效果容器 |
| A09 | 稳定度改为 135、-20，再手动加 5 | 真实显示；预览前不写入；确认后记录 before/after/delta |
| A10 | 一天内重复决议完成三次 | 三条独立记录，次数 3；无冷却或周期限制 |
| A11 | 决议未完成与放弃 | 未操作不记失败；放弃终止继续执行并保留历史次数 |
| A12 | 决议完成、放弃及误操作纠正 | 不修改国策与稳定度，不要求填写决策过程 |
| A13 | Trait 演化和国家精神归档 | 用户手工建关系；无正负分类、自动升级或自动效果；归档可查 |
| A14 | 多意识形态与哲学编辑 | 思想并存，无主项强制；哲学为一篇文档 |
| A15 | 文本连续自动保存 | 当前正文更新；不自动产生文本快照或事实/风格双存 |
| A16 | 主动保存世界与文本快照 | 可命名和描述；正文及世界冻结；不包含待接受 Proposal |
| A17 | 保存快照后修改/删除原对象和图片 | 旧快照仍显示当时状态；回溯只读，不覆盖当前 Life |
| A18 | 检查 Snapshot 内容 | 包含当时世界，不递归复制 Archive、事件/随笔库和旧快照 |
| A19 | Archive 查看、筛选和编辑源对象 | 聚合已有记录，无 Archive 复制实体表和双写 |
| A20 | 从国策按钮生成多个下一节点候选 | 独立预览，不自动生成整树；取消后 DB/画布/备份无候选 |
| A21 | 修改候选后选择接受一部分 | 仅所选部分落库；用户确认状态和关系；事务失败不留半个节点 |
| A22 | 生成期间切换 Life 或删除锚点 | 旧响应不能写入新 Life；接受前重新核对对象有效性 |
| A23 | 选择写作风格后接受润色 | 只保存确认正文；没有随时切风格显示的双正文模型 |
| A24 | 总参谋部勾模块和设置轮数 | 实际发送范围匹配预览；成员确实回应此前发言；达到上限停止 |
| A25 | 会议取消、失败、正常结束 | 不擅自继续；纪要确认后入档；没有替用户执行决定 |
| A26 | API 超时、无效结构、取消或无网络 | 原正文/世界不变，手工本地功能仍可用 |
| A27 | 双击、重复网络回调和真实多次完成 | 同次请求防重；不同用户操作可各记一次 |
| A28 | 删除与画布 Ctrl+Z | 删除范围明确，相关引用可读，撤销后画面与 DB 一致 |
| A29 | 备份恢复到测试目录/实例 | 数据、图片、关系可用，API Key 不在备份，恢复失败可回退 |
| A30 | 自动备份关闭与开启 | 按设置运行，状态可见，无强制后台人生分析 |
| A31 | 旧数据库 migration 与失败注入 | 成功保留旧资料；失败不清库，可恢复 |
| A32 | Markdown/JSON 导出及秘密检查 | 内容可读、结构带版本、Life 范围正确，无密钥或候选泄漏 |
| A33 | 超事件展示和重播 | 图片/标题/日期正常，不重复记录或修改世界 |
| A34 | Windows 11 安装、重启、离线 | 核心本地工作流完成；视觉层次明确；无需账号和同步 |

Definition of Done：v1 范围完成，上述关键行为通过适当的单元/集成与人工验收；无已知的数据隔离、未确认 AI 写入、历史丢失、备份不可恢复问题。依赖版本与迁移脚本可复现，不能用截图或仅 UI 演示代替持久化验证。

## 19. Review 追溯与旧设计处置

### 19.1 本次 19 项要求逐条核对

| 用户要求 | 定稿位置 | Review 结论 |
|---|---|---|
| 1 整个对话最终需求逐条对照 | 本节及 19.2 | 已读全部可访问对话，按后续修正覆盖；原文件逐行比对受限 |
| 2 清理被否定/收紧的旧设计 | 19.3 | 已从有效规格排除，旧设计仅在 Review 对照中出现 |
| 3 Archive 展示层 | §10、§14 | 无复制实体表 |
| 4 事件无自动效果，之后手动改状态 | §9.1、§14.3 | 来源关联不执行状态变更 |
| 5 无命名 FocusTree，单大画布 | §7.1、§14 | 每个 Life 一个画布，focus/focus_relation |
| 6 互斥由用户判断 | §7.3 | 无现实判断、自动警告或阻止 |
| 7 用户按钮试探生成下一节点 | §7.4、§12 | 候选预览；接受前无持久化；不续写整树 |
| 8 总参谋部上下文按模块 | §12.2、§12.4 | v1 不做逐对象勾选器 |
| 9 风格生成时选择，只保存定稿 | §11.2、§12.3 | 无强制双存；主动文本快照作为明确独立行为 |
| 10 决议独立，无周期，可无限完成 | §8 | 放弃为不再做；无国策子任务与决策流程 |
| 11 稳定度无自动规则、可超界 | §6.5 | 真实数值与手工变更历史 |
| 12 国家精神/Trait 不分正负 | §6.1、§6.4、§14 | 领域与字段均排除正负分类 |
| 13 Snapshot/Life 区分 | §4、§11 | 独立空间与命名冻结状态不同 |
| 14 底层多 Life | §3、§14 | 关系、查询、AI、快照、导出均隔离 |
| 15 Windows-first、本地、云 API、无同步 | §3、§12、§13 | v1 明确边界 |
| 16 指定栈与适度架构 | §13、§14、§17 | 必须基础分层，未来设施不强制 |
| 17 中文主体英文代码术语 | 全文 | 已统一 |
| 18 一份 Markdown Source of Truth | 本文件 | 不另交 DOCX 或独立 Review 报告 |
| 19 一致性检查与 Review 说明 | §19.4 | 完成文档层检查，未冒充应用实测 |

### 19.2 原对话用户回答追溯

以下短 ID 为原会话 turn ID 前八位，可与原对话定位；按先后顺序列出实际含产品需求的用户回答。早期被覆盖的回答保留在此表作追溯，不作为有效实现要求。

| Turn | 用户回答关键内容 | 最终归并 |
|---|---|---|
| `bbb21739` | 自用、长期政策状态、关键信息生成国策、宏大叙事与参考 | §2、§6、§7；早期自动扣稳定度和随机惩罚被后续无自动规则覆盖 |
| `bbb21dcb` | 不做流水账、用户标记、定性推演、AI 可反驳、总参谋部不代决策 | §2、§9.3、§12；Todo 挂载的早期解释被后续纠正 |
| `ccffe734` | 历史追溯、自由文本、哲学分开、国家精神自己写、无隐藏国策、多人格冲突 | §6、§7、§12；警告限定到后续用户按钮制 |
| `b7d81a39` | 无时代、当前局势、所有文本可快照、统一国策、事件可关联 | §6、§9、§11；“很多棵树”由后续单画布解释覆盖 |
| `e9f4647b` | 主动快照、描述、普通/超事件、四状态、无计划中、前置不阻止、无进度 | §7、§9、§11；未来推演进随笔 |
| `a68b186d` | 事件状态修改由用户填、Markdown 随笔到处挂载、固定参谋、叙事首页 | §5、§9、§12；事件工作流按后续“先写再修改”收紧 |
| `2fb7f10e` | 意识形态纯文本、Trait 卡片、决议表现、不做控制台、个人使用 | §3、§6、§8；国家精神分类被后续取消 |
| `7aaec550` | 决议不挂国策、Win11、本地、云 API、必须拖拽、五核心模块 | §3、§7、§8、§13 |
| `9dfe3671` | v1 跨设备成本大，倾向不做 | §3、§13；不沿用早期必须同步 |
| `4279f552` | 稳定度手调、国策日期、纯文字精神、Trait 谱系、保留节点历史、叙事包装 | §6、§7、§12；互斥警告被最后用户判断要求收紧 |
| `a1026d01` | 稳定度可回溯/无自动规则、叙事不偏事实、无历史页和搜索、互相讨论、现实时间快照 | §6、§9—§12；全局搜索的早期同意被撤回 |
| `a8eaf3f2` | 快照冻结/命名/删除、先写事件再改状态、图片、轮数与上限、会议入档、决议无条件 | §8—§12、§16 |
| `9c0ac295` | 无决策过程、统一 Archive、快照不复制档案、固定首页、通用附件、自由布局建议 | §5、§7—§11、§13—§16 |
| `0ddac8bd` | 稳定度可超界、Trait/精神不分类、国策图片/并行、无树名、重复决议、精神无日期/归档、多意识形态、长期哲学 | §6—§8、§14 |
| `9d971ce3` | 多 Life、稳定度真实显示、国策历史/可逆、重复无时间间隔、放弃不再做、主题预留 | §3、§6—§8、§13—§14 |
| `f853862a` | 删除归档并存、普通编辑自动保存、Ctrl+Z、秘密存储、成员模型配置、轮数、图片优先、自动备份开关 | §12、§15—§16 |
| `15ffd901` | Archive 统一、Trait 无合流、冲突用户判断、模块上下文、风格不随时切/不双存、排版自行设计 | §5—§7、§10、§12 |
| `4e14e6b5` | 未否定且后续承接的一些建议也是接受 | §1，采用承接证据而非把所有沉默当同意 |
| `45702d6d` | 对英文规格提出疑问 | 中文正文与英文术语；本次明确确认此格式 |
| `35c9d065` | 汇总所有上下文写文档 | 本文件整体 |
| `36a174c6` | 询问双格式及是否已 Review | 本次只交一份，披露原文件不可读限制 |
| `527d4809` | 用户可选 AI 试探生成下一国策节点 | §7.4；覆盖可能把 AI 限得过窄的旧描述 |
| `e59ed2af` | Review 后定稿 | 本文件与本节 Review 说明 |

其余四个 turn 为最初发想、请求提问、请求指出疑惑和催问完成进度，不额外构成未纳入的功能决定。原对话中助手提出但未被后续承接的概念，不凭出现过就进入 v1。

### 19.3 已删除或覆盖的旧设计

| 旧设计 | 最终处理 |
|---|---|
| 未完成任务扣稳定度、拒绝随机事件扣分、事件自动增减属性 | 删除；稳定度与状态由用户分别确认修改 |
| AI 自动判断互斥、启动时冲突警告、前置阻止启动 | 删除自动判断与阻止；只表达关系，AI 分析需按钮 |
| 多个命名国策树、树描述图标、树版本与复制路线 | 删除领域实体；每个 Life 单画布，多个节点群自然并存 |
| 决议挂载国策、自动解锁决议、决策过程对象 | 删除；Decision 是独立小待办 |
| 每日/每周重复、冷却、漏做一期、周期放弃 | 删除；用户每按一次代表一次完成，放弃终止整个决议 |
| 国策计划中、自动进度、持续时间完成机制 | 删除；仅四状态，用户判断完成 |
| 国家精神及 Trait 正/中/负分类、技能 modifier 与自动演化 | 删除；用户文字、自建谱系，无合流工程 |
| 单一人生数据库、不支持多 Life | 覆盖；v1 数据层正式支持并隔离多个 Life |
| 事实稿+风格稿强制双存、随时切风格重新渲染 | 删除；生成时选风格，保存确认正文 |
| 每次编辑自动版本、自动世界快照、快照复制全 Archive | 删除；主动快照，世界状态与历史库边界明确 |
| 独立历史页、全局搜索、逐对象 AI 上下文勾选 | 删除 v1 要求；Archive 轻筛选、上下文按模块 |
| 事件编辑器可执行结构化 effects | 删除；先记录事件，再到模块手工修改并关联来源 |
| 同时跨平台与同步、完整 Mod/插件、复杂基础设施 | 移出 v1；保留清晰边界与可迁移数据 |
| AI 完全不能建议后续国策 | 修正；允许用户按钮触发一个或多个下一节点候选 |

### 19.4 一致性检查结论与实现约定

本次对产品规则、工作流、逻辑 Schema、开发阶段和验收表进行了交叉检查，未发现仍然并列生效的明显矛盾。重点消解如下：

- **AI 候选与自动保存：**候选内存隔离，接受后才成为可自动保存的正式正文；会议纪要同样先确认。
- **下一节点与无计划中：**接受时用户选择已有四状态，不偷增第五种状态，不默认开始。
- **事件可关联状态变化与事件无效果：**来源不执行操作，实际变化来自独立用户操作。
- **互斥存在与不阻止：**关系只展示用户理解，不由程序判定现实冲突；结构有效性校验仍可执行。
- **世界冻结与 Archive 不复制：**冻结当时世界值，不递归复制历史库；其他正文可由用户主动文本快照保留。
- **历史保留与错误可删：**归档保留，删除可纠错；当前对象删除不自动擦除快照和备份副本。
- **文本单正文与可回溯：**不强制事实/风格双存，用户主动留稿属于独立快照行为。
- **重复决议无限完成与防重：**按操作请求防重复处理，不按天或时间窗口限次。
- **多 Life 与本地自用：**空间隔离不需要账号、服务器或同步。
- **快照回溯与备份恢复：**前者 v1 只读观察，后者是确认后的整体验证与恢复流程。
- **可扩展与 v1 克制：**Repository/Service、migration、adapter 为当前必需；Mod/同步运行时没有进入验收。

未在原对话最终确定的行为已明确处理，而非包装为用户确认：Snapshot 一键覆盖当前世界不纳入 v1；多 Life 的简洁 UI、初始稳定度可未设置、决议误操作纠正、会话内撤销栈、上下文溢出处理和具体表字段属于本文实现约定；备份频率、保留数量、会议轮数硬上限和依赖小版本在实现时配置并验证。

**Review 最终结论：**本文可以作为依据可访问需求对话重建的 v1 实现依据，已覆盖本次 19 项要求的规格内容；唯一无法完成的审查环节是原生成文件的逐行核对。若以后取得原文件，仅需核验是否存在尚未展示且有效的内容；不得借此恢复本文已明确废弃的旧设计。
